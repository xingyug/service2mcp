apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: example-targets
  labels:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/part-of: tool-compiler
    tool-compiler.io/protocol: openapi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gitea
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
