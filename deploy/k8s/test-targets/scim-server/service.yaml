apiVersion: v1
kind: Service
metadata:
  name: scim-server
  namespace: tool-compiler-test-targets
  labels:
    app: scim-server
    app.kubernetes.io/name: scim-server
    app.kubernetes.io/part-of: tool-compiler-test-targets
    app.kubernetes.io/component: test-target
    tool-compiler.io/protocol: scim
spec:
  type: ClusterIP
  selector:
    app: scim-server
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
