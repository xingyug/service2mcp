apiVersion: apps/v1
kind: Deployment
metadata:
  name: soap-server
  namespace: example-test-targets
  labels:
    app: soap-server
    app.kubernetes.io/name: soap-server
    app.kubernetes.io/part-of: example-test-targets
    app.kubernetes.io/component: test-target
    tool-compiler.io/protocol: soap
spec:
  replicas: 1
  selector:
    matchLabels:
      app: soap-server
  template:
    metadata:
      labels:
        app: soap-server
        app.kubernetes.io/name: soap-server
        app.kubernetes.io/component: test-target
        tool-compiler.io/protocol: soap
    spec:
      containers:
        - name: soap-server
          image: YOUR_REGISTRY/YOUR_PROJECT/test-target-soap:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
