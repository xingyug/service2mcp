# Tool Compiler v2 bug scan ledger

Created on `2026-03-29` during a dedicated backend-to-frontend scan.

This file is intentionally newly created for this scan and is not reusing any prior agent bug list.

## Scope

- Read all documents listed in `new-agent-reading-list.md`
- Scan backend services, shared libraries, deployment-supporting code, and frontend UI
- Record bugs only; do not modify code in this phase

## Evidence standard

Each entry should include:

- Bug ID
- Area
- Severity
- File(s)
- Summary
- Evidence
- Why it is a bug
- Suggested validation path

## Findings

Findings will be appended below as the scan progresses.

### Round 1 - contract, auth, and runtime regressions

#### Frontend <-> compiler API contract bugs

| ID | Sev | Files | Finding |
| --- | --- | --- | --- |
| BUG-001 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/compilations.py` | `compilationApi.list()` calls `GET /api/v1/compilations`, but the backend exposes only `POST /api/v1/compilations`, `GET /api/v1/compilations/{job_id}`, and `GET /api/v1/compilations/{job_id}/events`; `/compilations` and dashboard pages will 404. |
| BUG-002 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/compilations.py` | `compilationApi.retry()` targets `POST /api/v1/compilations/{job_id}/retry`, but no retry route exists in the compiler API. |
| BUG-003 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/compilations.py` | `compilationApi.rollback()` targets `POST /api/v1/compilations/{job_id}/rollback`, but no rollback route exists in the compiler API. |
| BUG-004 | High | `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The frontend expects compilation jobs to expose `job_id`, but the backend serializes the field as `id`; every consumer reading `job.job_id` is reading a non-existent property. |
| BUG-005 | High | `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The frontend job type assumes `failed_stage`, `completed_at`, and `artifacts`, but `CompilationJobResponse` does not return those fields. |
| BUG-006 | High | `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The frontend expects `error_message`, but the backend serializes job failures as `error_detail`; failed-job UIs will never show the actual backend error text. |
| BUG-007 | High | `apps/web-ui/src/components/compilations/compilation-wizard.tsx`, `apps/compiler_api/models.py` | The wizard redirects with `result.job_id`, but create-compilation responses return `id`; successful submissions navigate to `/compilations/undefined`. |
| BUG-008 | High | `apps/web-ui/src/app/(dashboard)/compilations/page.tsx`, `apps/web-ui/src/types/api.ts` | The compilation list page filters, keys, links, and actions by `job.job_id`, so it cannot render or operate on real backend job responses. |
| BUG-009 | High | `apps/web-ui/src/app/(dashboard)/compilations/[jobId]/page.tsx`, `apps/web-ui/src/types/api.ts` | The compilation detail page displays and copies `job.job_id`, which does not exist on the backend response. |
| BUG-010 | High | `apps/web-ui/src/app/(dashboard)/compilations/[jobId]/page.tsx`, `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The detail page also depends on missing `completed_at`, `failed_stage`, `error_message`, and `artifacts`, so large sections of the page cannot reflect real compiler API data. |
| BUG-011 | High | `apps/web-ui/src/lib/hooks/use-sse.ts`, `apps/compiler_api/routes/compilations.py` | The SSE hook only listens on `EventSource.onmessage`, but the server emits named SSE events (`event: stage_started`, etc.); custom events will never be delivered to the UI. |
| BUG-012 | Medium | `apps/web-ui/src/lib/hooks/use-sse.ts`, `apps/web-ui/src/app/(dashboard)/compilations/[jobId]/page.tsx` | The detail page only opens the SSE stream while a job is running, so completed and failed jobs always show an empty event log even though the backend stores event history. |
| BUG-013 | Medium | `apps/web-ui/src/lib/hooks/use-sse.ts` | `useCompilationEvents()` calls `setState()` during render when `jobId` changes (`prevJobId !== jobId`), which is a React anti-pattern that can trigger extra renders and warnings. |
| BUG-014 | High | `apps/web-ui/src/components/compilations/compilation-wizard.tsx`, `apps/compiler_api/models.py` | The wizard attaches `auth_config` to the create request, but `CompilationCreateRequest` is `extra="forbid"` and has no `auth_config` field; authenticated compilations will 422. |
| BUG-015 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/services.py` | `serviceApi.get()` targets `GET /api/v1/services/{serviceId}`, but the backend only exposes `GET /api/v1/services`; the service detail page has no working backend endpoint. |
| BUG-016 | High | `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The frontend service type expects `name`, `version_count`, and `last_compiled`, but the backend returns `service_name`, `tool_count`, and `created_at`. |
| BUG-017 | High | `apps/web-ui/src/app/(dashboard)/services/page.tsx`, `apps/web-ui/src/types/api.ts` | The services page renders `s.name`, `s.version_count`, and `s.last_compiled`, so real backend responses produce blank names and incorrect version/recency values. |
| BUG-018 | High | `apps/web-ui/src/app/(dashboard)/services/[serviceId]/page.tsx`, `apps/web-ui/src/types/api.ts` | The service detail page header uses `service.name` and `service.version_count`, neither of which exists in the backend service summary response. |
| BUG-019 | Medium | `apps/web-ui/src/app/(dashboard)/page.tsx`, `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The dashboard computes "Active Tools" from `version_count` and "recently compiled" from `last_compiled`, but the backend provides `tool_count` and `created_at`; the headline metrics are wrong even before rendering issues. |
| BUG-020 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/artifacts.py` | `artifactApi.listVersions()` calls `/api/v1/services/{serviceId}/versions`, but the backend route lives under `/api/v1/artifacts/{service_id}/versions`. |
| BUG-021 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/artifacts.py` | `artifactApi.getVersion()` also points at `/api/v1/services/...`, while the real backend endpoint is `/api/v1/artifacts/{service_id}/versions/{version_number}`. |
| BUG-022 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/artifacts.py` | `artifactApi.diff()` calls `/api/v1/services/{serviceId}/diff`, but the backend exposes `/api/v1/artifacts/{service_id}/diff`. |

#### Frontend auth, policy, audit, and gateway bugs

| ID | Sev | Files | Finding |
| --- | --- | --- | --- |
| BUG-023 | Critical | `apps/web-ui/src/stores/auth-store.ts`, `apps/web-ui/src/lib/api-client.ts` | The auth store persists under `auth-storage`, but `getAuthToken()` reads `localStorage["auth_token"]`; successful logins never attach Authorization headers to API requests. |
| BUG-024 | High | `apps/web-ui/src/app/(auth)/login/page.tsx`, `apps/web-ui/next.config.ts` | The login page posts to relative `/api/v1/authn/validate`, but there is no Next.js rewrite/proxy; in the browser this hits the web app origin, not the access-control service. |
| BUG-025 | High | `apps/web-ui/src/app/(auth)/login/page.tsx`, `apps/access_control/authn/routes.py`, `apps/access_control/authn/models.py` | Password login sends a `Basic` Authorization header and no JSON body, but `/api/v1/authn/validate` expects a JSON payload `{ "token": ... }`; the route will reject the request before auth logic runs. |
| BUG-026 | High | `apps/web-ui/src/app/(auth)/login/page.tsx`, `apps/access_control/authn/routes.py`, `apps/access_control/authn/models.py` | PAT login sends a bearer header and no JSON body to the same body-driven validation endpoint, so PAT sign-in is also incompatible with the backend contract. |
| BUG-027 | High | `apps/web-ui/src/app/(auth)/login/page.tsx`, `apps/access_control/authn/models.py` | The login page expects `token`, `username`, `email`, and `roles` in the validation response, but the backend returns `subject`, `token_type`, and raw `claims`. |
| BUG-028 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authn/routes.py` | `authApi.validateToken()` uses `/api/v1/auth/validate`, but the backend authn router is mounted at `/api/v1/authn`. |
| BUG-029 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authn/routes.py` | `authApi.createPAT()` uses `/api/v1/auth/pats`, but the real PAT creation endpoint is `/api/v1/authn/pats`. |
| BUG-030 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authn/routes.py`, `apps/access_control/authn/models.py` | `authApi.listPATs()` omits the required `username` query parameter and expects a `{ pats: [...] }` payload, while the backend requires `username` and returns `{ items: [...] }`. |
| BUG-031 | High | `apps/web-ui/src/app/(dashboard)/pats/page.tsx`, `apps/access_control/authn/models.py` | The PAT page keys and revokes rows by `pat.pat_id`, but backend PAT responses expose `id`, not `pat_id`. |
| BUG-032 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authn/routes.py` | `authApi.revokePAT()` uses `/api/v1/auth/pats/{pat_id}/revoke`, but the backend route is `/api/v1/authn/pats/{pat_id}/revoke`. |
| BUG-033 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authz/routes.py` | All policy API calls use `/api/v1/policies`, but the backend authz router is mounted under `/api/v1/authz`. |
| BUG-034 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authz/routes.py` | The frontend updates policies with `PATCH`, but the backend only exposes `PUT /api/v1/authz/policies/{policy_id}`. |
| BUG-035 | High | `apps/web-ui/src/components/review/approval-workflow.tsx`, `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/authz/models.py`, `apps/access_control/authz/routes.py` | Policy evaluation posts to the wrong path and omits the required `risk_level` field; backend evaluation requests cannot validate. |
| BUG-036 | High | `apps/web-ui/src/types/api.ts`, `apps/access_control/authz/models.py` | The frontend policy list type expects `{ policies: [...] }`, but the backend returns `{ items: [...] }`. |
| BUG-037 | High | `apps/web-ui/src/app/(dashboard)/policies/page.tsx`, `apps/access_control/authz/models.py` | The policies page edits and deletes by `policy.policy_id`, but backend policy responses expose `id`; edit/delete actions are wired to a non-existent field. |
| BUG-038 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/audit/routes.py` | `auditApi.list()` calls `/api/v1/audit`, but the backend list endpoint is `/api/v1/audit/logs`. |
| BUG-039 | Medium | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/audit/routes.py` | The frontend sends `since`/`until` audit filters, but the backend route accepts `start_at`/`end_at`; time filtering does not line up. |
| BUG-040 | High | `apps/web-ui/src/types/api.ts`, `apps/access_control/audit/models.py` | The frontend expects audit list payloads as `{ entries: [...] }`, while the backend returns `{ items: [...] }`. |
| BUG-041 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/audit/routes.py` | `auditApi.get()` targets `/api/v1/audit/{entryId}`, but the backend exposes no single-entry audit route at all. |
| BUG-042 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/gateway_binding/routes.py` | `gatewayApi` points at the compiler API base URL even though all gateway-binding routes are served by the access-control service. |
| BUG-043 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/gateway_binding/routes.py` | `gatewayApi.reconcile()` calls `/api/v1/gateway/reconcile` and expects `{ synced, deleted, errors }`, but the backend route is `/api/v1/gateway-binding/reconcile` and returns six separate counters. |
| BUG-044 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/gateway_binding/routes.py` | `gatewayApi.setRoute()` posts `{ service_id, version_number, route_config }` to `/api/v1/gateway/routes`, but the backend sync route is `/api/v1/gateway-binding/service-routes/sync` and expects `{ route_config, previous_routes }`. |
| BUG-045 | High | `apps/web-ui/src/lib/api-client.ts`, `apps/access_control/gateway_binding/routes.py` | `gatewayApi.deleteRoute()` issues `DELETE /api/v1/gateway/routes/{serviceId}`, but the backend delete path is `POST /api/v1/gateway-binding/service-routes/delete` with a JSON body. |
| BUG-046 | Critical | `apps/web-ui/src/app/(dashboard)/gateway/page.tsx`, `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py` | The gateway page builds route URIs with `svc.name.toLowerCase()`, but compiler service summaries do not return `name`; once real service data loads, this code can throw on `undefined.toLowerCase()`. |
| BUG-047 | Medium | `apps/web-ui/src/app/(dashboard)/gateway/page.tsx` | The gateway page fabricates route status using `i % 5` / `i % 3` and hard-coded history records instead of backend data, so the UI can report drift/errors that do not exist and hide real gateway state. |

#### Backend auth, consistency, and review-flow bugs

| ID | Sev | Files | Finding |
| --- | --- | --- | --- |
| BUG-048 | Critical | `apps/access_control/authn/routes.py` | PAT creation logs a warning and still returns success when gateway consumer sync fails, creating split-brain between stored PATs and actual gateway auth state. |
| BUG-049 | Critical | `apps/access_control/authn/routes.py` | PAT revocation also swallows gateway sync failures and still returns a successful revoke response, so revoked PAT state can diverge between DB and gateway. |
| BUG-050 | Critical | `apps/access_control/authz/routes.py` | Policy creation returns success even when gateway policy sync fails, leaving the policy store and enforced gateway state inconsistent. |
| BUG-051 | Critical | `apps/access_control/authz/routes.py` | Policy updates also swallow gateway sync errors and still report success, so operators can believe a policy changed when enforcement did not. |
| BUG-052 | Critical | `apps/access_control/authz/routes.py` | Policy deletion reports success even when gateway deletion fails, which can leave stale gateway bindings active after the DB record is gone. |
| BUG-053 | Critical | `apps/access_control/authn/routes.py` | `POST /api/v1/authn/pats` has no authentication/authorization dependency, so any caller can mint PATs for arbitrary usernames. |
| BUG-054 | Critical | `apps/access_control/authn/routes.py` | `GET /api/v1/authn/pats` is unauthenticated and accepts any `username`, so callers can enumerate PAT metadata for other users. |
| BUG-055 | Critical | `apps/access_control/authn/routes.py` | `POST /api/v1/authn/pats/{pat_id}/revoke` is unauthenticated, so any caller with a PAT UUID can revoke it. |
| BUG-056 | Critical | `apps/access_control/authz/routes.py` | `POST /api/v1/authz/policies` is unauthenticated, so policy creation is open to arbitrary callers. |
| BUG-057 | High | `apps/access_control/authz/routes.py` | `GET /api/v1/authz/policies` is unauthenticated, exposing policy inventory without caller checks. |
| BUG-058 | High | `apps/access_control/authz/routes.py` | `GET /api/v1/authz/policies/{policy_id}` is unauthenticated. |
| BUG-059 | Critical | `apps/access_control/authz/routes.py` | `PUT /api/v1/authz/policies/{policy_id}` is unauthenticated, so arbitrary callers can mutate policy decisions. |
| BUG-060 | Critical | `apps/access_control/authz/routes.py` | `DELETE /api/v1/authz/policies/{policy_id}` is unauthenticated, so arbitrary callers can remove policy rules. |
| BUG-061 | Critical | `apps/access_control/gateway_binding/routes.py` | `POST /api/v1/gateway-binding/reconcile` is unauthenticated, allowing arbitrary callers to trigger gateway reconciliation. |
| BUG-062 | Critical | `apps/access_control/gateway_binding/routes.py` | `POST /api/v1/gateway-binding/service-routes/sync` is unauthenticated, so callers can publish routes without auth checks. |
| BUG-063 | Critical | `apps/access_control/gateway_binding/routes.py` | `POST /api/v1/gateway-binding/service-routes/delete` is unauthenticated, so callers can remove routes without auth checks. |
| BUG-064 | Critical | `apps/access_control/gateway_binding/routes.py` | `POST /api/v1/gateway-binding/service-routes/rollback` is unauthenticated, so callers can roll routes back without auth checks. |
| BUG-065 | High | `apps/access_control/main.py` | `/readyz` returns `{"status":"not_ready"}` after DB failures but never sets a non-2xx status code, so Kubernetes/readiness probes can treat an unhealthy service as ready. |
| BUG-066 | High | `apps/web-ui/src/stores/workflow-store.ts` | Review workflow state is stored only in persisted browser-side Zustand storage, so approval state is not durable or shared across users/browsers. |
| BUG-067 | High | `apps/web-ui/src/stores/workflow-store.ts`, `apps/web-ui/src/components/review/approval-history.tsx` | Approval history is also browser-local, meaning audit/review history disappears outside the originating browser profile. |
| BUG-068 | High | `apps/web-ui/src/components/review/review-status-badge.tsx`, `apps/web-ui/src/stores/workflow-store.ts` | Review badges derive status from the local store rather than backend state, so different clients can show different review states for the same service version. |
| BUG-069 | High | `apps/web-ui/src/components/review/approval-workflow.tsx`, `apps/web-ui/src/stores/workflow-store.ts` | Approval transitions only mutate local Zustand state; there is no backend API call, so "approved/published/deployed" status changes are UI-only illusions. |
| BUG-070 | High | `apps/web-ui/src/components/services/ir-editor.tsx` | `IREditor` "Save" only calls an optional local callback; it has no persistence path to the compiler/registry backend. |
| BUG-071 | High | `apps/web-ui/src/app/(dashboard)/services/[serviceId]/review/page.tsx`, `apps/web-ui/src/components/services/ir-editor.tsx` | The review page renders `IREditor` without `onSave`, so clicking "Save" exits edit mode but never updates any shared state or backend artifact. |

#### Runtime/deploy regression and test-suite drift bugs

| ID | Sev | Files | Finding |
| --- | --- | --- | --- |
| BUG-072 | Critical | `tests/integration/test_compiler_worker_activities.py`, `libs/generator/generic_mode.py`, `libs/generator/templates/configmap.yaml.j2` | The in-memory deploy harness still expects `config_map["data"]["service-ir.json"]`, but generated manifests now store gzipped IR under `binaryData["service-ir.json.gz"]`; deploy-stage integration now crashes with `KeyError: 'data'`. |
| BUG-073 | High | `libs/generator/generic_mode.py`, `libs/generator/templates/configmap.yaml.j2`, `tests/integration/test_compiler_worker_activities.py` | Generic manifest generation and the activity test harness have drifted on both storage field (`binaryData` vs `data`) and filename (`service-ir.json.gz` vs `service-ir.json`). |
| BUG-074 | Critical | `tests/integration/test_compiler_worker_activities.py`, `apps/compiler_worker/workflows/compile_workflow.py` | The manifest contract drift breaks `test_default_activity_registry_executes_full_pipeline_with_runtime_harness`; the workflow now fails at `deploy` in the default activity registry path. |
| BUG-075 | Critical | `tests/integration/test_compiler_worker_activities.py`, `apps/compiler_worker/workflows/compile_workflow.py` | The same drift also breaks `test_default_activity_registry_waits_for_runtime_readiness_before_validation`, so runtime-readiness validation coverage is currently dead. |
| BUG-076 | Critical | `tests/integration/test_compiler_worker_activities.py`, `apps/compiler_worker/workflows/compile_workflow.py` | The same drift also breaks `test_default_activity_registry_publishes_routes_via_access_control`, so route-publication integration coverage is currently broken. |
| BUG-077 | Critical | `tests/integration/test_compiler_worker_activities.py`, `apps/compiler_worker/workflows/compile_workflow.py` | The same drift also breaks `test_route_rollback_restores_previous_active_route_when_register_fails`, so rollback recovery coverage is currently broken. |
| BUG-078 | Medium | `tests/contract/test_local_dev_assets.py`, `scripts/smoke-gke-llm-e2e.sh` | The contract test still asserts the old six-protocol allowlist string `all|graphql|rest|grpc|soap|sql`, but the smoke script now supports `openapi`, `jsonrpc`, `odata`, and `scim`; CI fails because the test was not updated with the new script contract. |
| BUG-079 | Medium | `apps/web-ui/src/app/(auth)/__tests__/login-page.test.tsx`, `apps/web-ui/src/app/(auth)/login/page.tsx`, `apps/access_control/authn/routes.py` | The login page tests explicitly bless a Basic-auth flow and `token/username/roles` response shape that the backend validate endpoint does not implement, so the frontend suite passes while the real login contract is broken. |
| BUG-080 | Medium | `apps/web-ui/src/hooks/__tests__/use-api.test.tsx`, `apps/web-ui/src/lib/__tests__/api-client.test.ts`, `apps/web-ui/src/lib/api-client.ts`, `apps/compiler_api/routes/*.py`, `apps/access_control/*/routes.py` | The frontend hook/client tests mock and assert non-existent endpoints and payload shapes (`/api/v1/services/{id}`, `/api/v1/policies`, `/api/v1/audit`, `service_id` in compilation create, etc.), masking real integration breakages behind green mocked tests. |

#### Registry scoping, transaction, and runtime capability bugs

| ID | Sev | Files | Finding |
| --- | --- | --- | --- |
| BUG-081 | Critical | `libs/db_models.py` | The partial unique index `uq_service_versions_one_active` is keyed only on `service_id`, so different tenants/environments cannot each hold an active version of the same service without conflicting at the database level. |
| BUG-082 | High | `libs/db_models.py` | `uq_service_version` is also scoped only to `(service_id, version_number)`, so the same logical version number cannot exist in different tenants/environments even though the schema stores `tenant` and `environment` columns. |
| BUG-083 | Critical | `apps/compiler_api/repository.py` | `_deactivate_service_versions()` clears `is_active` for every row with the same `service_id` and ignores `tenant`/`environment`, so one activation can silently deactivate other deployment scopes. |
| BUG-084 | Critical | `apps/compiler_api/repository.py` | `create_version()` calls `_deactivate_service_versions(service_id)` whenever a new version is marked active, so creating an active version in one tenant/environment can deactivate the active version of another tenant/environment. |
| BUG-085 | Critical | `apps/compiler_api/repository.py` | `activate_version()` has no `tenant`/`environment` parameters and uses the unscoped deactivation path, so activating a version is a global service-wide flip rather than a scoped rollout. |
| BUG-086 | Critical | `apps/compiler_api/repository.py` | `delete_version()` promotes the highest remaining version using only `service_id`; deleting an active version can accidentally promote a version from a different tenant/environment. |
| BUG-087 | High | `apps/compiler_api/routes/artifacts.py` | `PUT /api/v1/artifacts/{service_id}/versions/{version_number}` exposes no `tenant`/`environment` filters even though the read endpoints support them, so updates are not scoped the same way reads are. |
| BUG-088 | High | `apps/compiler_api/routes/artifacts.py` | `POST /api/v1/artifacts/{service_id}/versions/{version_number}/activate` also exposes no `tenant`/`environment` filters, forcing activation to be global. |
| BUG-089 | High | `apps/compiler_api/routes/artifacts.py` | `DELETE /api/v1/artifacts/{service_id}/versions/{version_number}` exposes no `tenant`/`environment` filters, so deletion cannot be scoped to the same slice used by list/get/diff. |
| BUG-090 | High | `libs/registry_client/client.py` | `RegistryClient.update_version()` has no way to send `tenant`/`environment`, so even the official client cannot target the same filtered artifact slice that read APIs support. |
| BUG-091 | High | `libs/registry_client/client.py` | `RegistryClient.activate_version()` likewise cannot scope activation by `tenant`/`environment`. |
| BUG-092 | High | `libs/registry_client/client.py` | `RegistryClient.delete_version()` also cannot scope deletion by `tenant`/`environment`. |
| BUG-093 | High | `apps/compiler_worker/activities/production.py` | `_next_version_number()` allocates version numbers by `service_id` only, so separate tenant/environment pipelines share a single global version sequence instead of independent scoped versioning. |
| BUG-094 | High | `libs/registry_client/models.py`, `apps/compiler_api/repository.py` | `ArtifactVersionCreate`/`ArtifactVersionUpdate` allow top-level `tenant` to disagree with `ir_json.tenant`; catalog filtering uses the row column while the artifact body can claim a different tenant. |
| BUG-095 | High | `libs/registry_client/models.py`, `apps/compiler_api/repository.py` | The same mismatch is possible for `environment`, so stored metadata and embedded IR environment can diverge. |
| BUG-096 | Medium | `libs/registry_client/models.py`, `apps/compiler_api/models.py`, `apps/compiler_api/repository.py` | The payload also allows top-level `protocol` to disagree with `ir_json.protocol`; service summaries prefer the row value, so catalog data can disagree with the actual stored IR contract. |
| BUG-097 | Medium | `apps/compiler_api/routes/artifacts.py` | `create_artifact_version()` converts every `IntegrityError` into "Service version already exists", which misreports other constraint failures such as the one-active-version conflict. |
| BUG-098 | High | `apps/compiler_api/routes/compilations.py`, `apps/compiler_api/repository.py`, `apps/access_control/audit/service.py` | `create_compilation()` commits the job record before audit logging and worker enqueue, so submission is not atomic; later failures happen after part of the request is already durably committed. |
| BUG-099 | High | `apps/compiler_api/routes/compilations.py`, `apps/access_control/audit/service.py` | When enqueue fails after `audit_log.append_entry()` succeeds, the route deletes the job but leaves the audit record behind, creating audit rows that reference a job the API immediately removed. |
| BUG-100 | High | `apps/compiler_api/routes/compilations.py`, `apps/access_control/audit/service.py` | The failure path calls `repository.delete_job()` without `session.rollback()` first; if `append_entry()` fails during commit, the shared session can be left in a failed transaction state and the cleanup delete can fail too. |
| BUG-101 | Medium | `apps/compiler_api/repository.py` | `ServiceCatalogRepository.list_services()` hard-limits results to 1000 with no pagination or truncation indicator, so catalogs silently drop services after the first 1000 active rows. |
| BUG-102 | Medium | `apps/compiler_api/repository.py` | `ArtifactRegistryRepository.list_versions()` also hard-limits version history to 1000 with no pagination or warning, so older versions silently disappear from API responses. |
| BUG-103 | Medium | `apps/access_control/authn/service.py` | `list_pats()` truncates after 1000 PATs with no pagination or truncation signal, so accounts with larger token inventories get incomplete results. |
| BUG-104 | Medium | `apps/access_control/authz/service.py` | `list_policies()` truncates after 1000 policies with no pagination or truncation signal, so policy inventories silently become incomplete at scale. |
| BUG-105 | Medium | `apps/access_control/audit/service.py` | `list_entries()` truncates audit history after 1000 rows with no pagination or truncation signal, which is especially dangerous for an audit log because callers cannot detect the omission. |
| BUG-106 | High | `libs/validator/pre_deploy.py`, `libs/ir/models.py` | `PreDeployValidator._validate_event_support()` accepts native `grpc_stream` descriptors whenever the feature flag is enabled, but it never rejects `GrpcStreamMode.client` or `GrpcStreamMode.bidirectional`; those IRs can pass validation even though runtime support is missing. |
| BUG-107 | High | `apps/mcp_runtime/grpc_stream.py`, `libs/ir/models.py` | `GrpcStreamRuntimeConfig` exposes `server`, `client`, and `bidirectional` modes, but `ReflectionGrpcStreamExecutor` throws `ToolError` for anything except `server`; the schema advertises modes the runtime cannot execute. |
| BUG-108 | High | `apps/compiler_worker/executor.py`, `apps/compiler_worker/celery_app.py` | `resolve_compilation_executor()` constructs a fresh `DatabaseWorkflowCompilationExecutor` for each task, and each executor lazily creates its own cached SQLAlchemy engine with no disposal path; long-running workers can leak one engine/pool per task invocation. |
| BUG-109 | Medium | `apps/mcp_runtime/loader.py` | `load_service_ir()` gzip-decompresses any `.gz` payload with no decompressed-size guard, so a malformed or malicious IR artifact can expand into unbounded memory at runtime startup. |
| BUG-110 | High | `apps/compiler_worker/activities/production.py`, `libs/validator/post_deploy.py` | The production workflow uses `PostDeployValidator.validate()`, which smoke-tests only a single selected operation; a deployment can pass validation while most generated tools remain broken. |
| BUG-111 | Medium | `libs/validator/post_deploy.py` | `_validate_tool_listing()` only compares runtime tool names against expected operation IDs; it never validates descriptions, parameter schemas, or metadata, so contract mismatches inside `/tools` can still pass the gate. |
| BUG-112 | Medium | `libs/validator/post_deploy.py` | `_validate_tool_listing()` builds `listed_tools` with a dict keyed by tool name, so duplicate runtime tools with the same name are silently collapsed and cannot be detected by validation. |
| BUG-113 | Medium | `libs/validator/post_deploy.py` | Streaming smoke validation only checks that `events` is a list and `lifecycle` is a dict; it does not require any events or inspect `termination_reason`, so empty or prematurely terminated streams can still pass. |
| BUG-114 | High | `libs/enhancer/enhancer.py` | `IREnhancer.enhance()` swallows all batch-level LLM exceptions, and if every batch fails it returns the original IR with `operations_enhanced=0` instead of surfacing a total enhancement failure; callers cannot distinguish "no work needed" from "enhancer broken." |
| BUG-115 | Medium | `libs/enhancer/examples_generator.py` | `_generate_for_operation()` catches all LLM/example parsing failures and returns an empty example list, so systemic example-generation failures are silently downgraded to "no examples generated." |
| BUG-116 | Medium | `libs/extractors/rest.py` | `_ObservedEndpoint.freeze()` uses `self.confidence or 0.5`, which upgrades an explicitly observed `0.0` confidence endpoint to `0.5`; zero-confidence discoveries are silently promoted into plausible endpoints. |
| BUG-117 | Medium | `libs/extractors/rest.py` | `_ObservedEndpoint.freeze()` defaults an endpoint with no confirmed methods to `("GET",)`, turning "method unknown" into a concrete GET operation and risking false-positive read-only tool generation. |
| BUG-118 | Medium | `libs/extractors/base.py` | `TypeDetector.detect()` and `detect_all()` swallow extractor `detect()` exceptions and keep scanning; if the correct extractor fails transiently, the detector can silently misclassify the source as another protocol instead of surfacing the detection failure. |
| BUG-119 | High | `apps/mcp_runtime/loader.py` | `create_runtime_server()` disables DNS rebinding protection by default (`MCP_DISABLE_DNS_REBINDING_PROTECTION` defaults to `"true"`), so a runtime exposed outside the intended Kubernetes boundary starts in the less secure mode unless operators explicitly opt back in. |
| BUG-120 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/openapi.py` | `extract_stage()` runs inside the worker's async pipeline, but `OpenAPIExtractor._get_content()` performs blocking `httpx.get()` calls; URL-based OpenAPI extraction can stall the event loop and serialize unrelated async work. |
| BUG-121 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/graphql.py` | `GraphQLExtractor._get_content()` performs a blocking `httpx.post()` introspection request from the async compilation pipeline, so GraphQL extraction can block the worker event loop for up to the full request timeout. |
| BUG-122 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/grpc.py` | `GrpcProtoExtractor._get_content()` also performs blocking `httpx.get()` from the async worker path, so remote `.proto` extraction blocks the event loop. |
| BUG-123 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/odata.py` | `ODataExtractor._get_content()` performs blocking `httpx.get()` during async extraction, so OData metadata fetches block the worker event loop. |
| BUG-124 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/soap.py` | `SOAPWSDLExtractor._get_content()` performs blocking `httpx.get()` during async extraction, so WSDL fetches block the worker event loop. |
| BUG-125 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/jsonrpc.py` | `JsonRpcExtractor._get_content()` performs blocking `httpx.get()` during async extraction, so remote JSON-RPC/OpenRPC discovery blocks the worker event loop. |
| BUG-126 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/scim.py` | `SCIMExtractor._raw_content()` performs blocking `httpx.get()` during async extraction, so remote SCIM schema discovery blocks the worker event loop. |
| BUG-127 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/rest.py` | `RESTExtractor` uses a synchronous `httpx.Client` for crawl/probe logic and is invoked directly from async worker stages, so REST discovery can monopolize the event loop while probing endpoints. |
| BUG-128 | Medium | `apps/compiler_worker/activities/production.py`, `libs/extractors/base.py`, `libs/extractors/*.py` | `TypeDetector.detect()` runs every extractor probe sequentially; for URL sources this can stack multiple blocking 30-second network fetches before one protocol is selected, multiplying latency and event-loop blockage on misclassified inputs. |
| BUG-129 | High | `libs/extractors/openapi.py`, `libs/extractors/tests/test_openapi.py` | `_follow_ref()` replaces any external OpenAPI `$ref` with `{}` instead of resolving it or failing extraction, so specs that use external component files silently lose schemas/parameters during extraction. |
| BUG-130 | High | `libs/extractors/openapi.py`, `libs/extractors/tests/test_openapi.py` | `_follow_ref()` also returns `{}` for broken or non-dict internal `$ref` paths, silently erasing referenced schema fragments instead of surfacing an invalid spec error. |
| BUG-131 | High | `libs/extractors/grpc.py` | `_SERVICE_PATTERN`, `_MESSAGE_PATTERN`, and `_ENUM_PATTERN` use simple `re.DOTALL` regexes with no brace-depth awareness, so valid proto files with nested messages/enums can be truncated or misparsed when the first inner `}` closes the match. |
| BUG-132 | Medium | `libs/validator/post_deploy.py` | `validate_with_audit()` attaches the full `audit_summary` to the report but still computes `overall_passed` from only health/tool-listing/smoke results, so a report can say "passed" even when the audit found failed tools. |
| BUG-133 | High | `apps/mcp_runtime/circuit_breaker.py`, `apps/mcp_runtime/proxy.py` | Once `CircuitBreaker.record_failure()` opens a breaker, `before_request()` blocks all future calls and there is no half-open state, retry window, or time-based reset; an operation can remain permanently dead until the whole proxy instance is recreated. |
| BUG-134 | High | `apps/mcp_runtime/proxy.py`, `apps/mcp_runtime/tests/test_proxy_extended.py` | `_poll_async_job()` returns the raw poll response for any unrecognized status value; a `200` poll payload like `{ "status": "unknown_state" }` then flows through the normal success path instead of failing the async job contract. |
| BUG-135 | Medium | `apps/mcp_runtime/proxy.py` | `_consume_sse_stream()` and `_consume_websocket_stream()` default `idle_timeout_seconds` to `1.0` when descriptor metadata omits it, so low-frequency but healthy streams are terminated after one second of silence under default settings. |
| BUG-136 | Medium | `libs/validator/post_deploy.py` | `_supported_descriptor_for_operation()` silently returns the first matching supported descriptor when an operation has duplicates, so duplicate/ambiguous event descriptor wiring can slip through validation instead of failing fast. |
| BUG-137 | Medium | `libs/enhancer/tool_grouping.py` | `ToolGrouper.group()` catches any LLM call failure and returns `GroupingResult(groups=[], llm_calls=1)`; callers cannot distinguish "grouping unavailable/broken" from "service legitimately produced no groups." |
| BUG-138 | Medium | `libs/extractors/llm_seed_mutation.py` | `generate_seed_candidates()` collapses any LLM failure or parse failure into an empty candidate list, so seed mutation can fail completely while discovery proceeds as if there were simply no new paths to try. |
| BUG-139 | Medium | `libs/validator/llm_judge.py` | `LLMJudge.evaluate()` returns a zero-score evaluation when every batch fails, making catastrophic judge failure look like a normal empty evaluation result instead of a hard error. |
| BUG-140 | Medium | `libs/validator/llm_judge.py` | `LLMJudge.evaluate()` averages only the successfully parsed batches; if some batches fail, the report silently drops those tools from `tools_evaluated`, inflating quality metrics without any partial-failure signal. |
| BUG-141 | Medium | `libs/validator/llm_judge.py` | `_parse_judge_response()` defaults malformed numeric scores to `0.5` instead of rejecting the item, so invalid judge output is silently converted into mediocre-but-valid quality scores. |
| BUG-142 | Medium | `libs/extractors/scim.py`, `deploy/k8s/test-targets/scim-server/server.py` | `SCIMExtractor.extract()` expects resource definitions under `Resources`, so pointing it at the bundled standard `/ServiceProviderConfig` endpoint yields zero resource operations instead of useful discovery because `_extract_resources()` ignores top-level capability documents entirely. |
| BUG-143 | Medium | `libs/extractors/scim.py`, `deploy/k8s/test-targets/scim-server/server.py` | `_extract_service_provider_config()` only reads a synthetic `service_provider_config` wrapper, but the bundled SCIM server returns standard top-level `bulk`, `changePassword`, `filter`, etc. fields; real `/ServiceProviderConfig` responses therefore lose those capabilities during extraction. |
| BUG-144 | Medium | `libs/extractors/scim.py`, `deploy/k8s/test-targets/scim-server/server.py` | When fed the bundled `/ResourceTypes` response, `SCIMExtractor` treats each resource-type entry as if it carried schema `attributes`; real `ResourceType` objects only expose names/endpoints, so generated create/update operations end up with empty body parameter lists. |
| BUG-145 | High | `libs/extractors/odata.py` | `_build_entity_set_operations()` uses only `key_props[0]`, so OData entity sets with composite keys produce invalid paths and omit required key parameters for every key after the first. |
| BUG-146 | Medium | `libs/extractors/odata.py` | OData create operations mark every non-key property as `required=True` regardless of `Nullable`, so extracted IR over-constrains inserts and forces callers to send optional properties that the metadata explicitly allows to be omitted. |
| BUG-147 | Medium | `libs/extractors/odata.py` | `_parse_functions()` and `_parse_actions()` index metadata by simple operation name, so overloaded OData functions/actions overwrite one another and only the last signature survives in the generated IR. |
| BUG-148 | Medium | `libs/extractors/odata.py` | `_parse_functions()` discards `<ReturnType>` metadata completely, so extracted OData function-import operations lose their response shape even when the EDMX declares one. |
| BUG-149 | Medium | `libs/extractors/soap.py` | `SOAPWSDLExtractor.extract()` always selects `services[0]` and that service's first port, so multi-service or multi-port WSDLs silently drop every endpoint except the first one declared. |
| BUG-150 | Medium | `libs/extractors/soap.py` | `_parse_messages()` reads only the first `wsdl:part` from each message, so multipart SOAP messages lose additional inputs/outputs during extraction. |
| BUG-151 | Medium | `libs/extractors/soap.py` | `_extract_xsd_fields()` only walks `xsd:sequence`; WSDL/XSD payloads modeled with `xsd:all`, `xsd:choice`, or attributes are emitted without the missing fields, producing incomplete SOAP tool schemas. |
| BUG-152 | Medium | `libs/extractors/soap.py` | `_resolve_wsdl_fields()` falls back to `[]` when a referenced element/type cannot be resolved, so schema-resolution failures degrade into zero-parameter / no-response operations instead of failing extraction. |
| BUG-153 | High | `libs/extractors/sql.py` | `SQLExtractor.detect()` advertises `mysql` and `mariadb` URLs as supported, but `_to_async_url()` only knows how to convert PostgreSQL and SQLite URLs; MySQL/MariaDB sources therefore detect successfully and then fail at extract time with `Unsupported SQL database URL`. |
| BUG-154 | High | `libs/extractors/sql.py` | `SQLExtractor.extract()` writes the full database URL into `source_url`, `base_url`, and the schema fingerprint payload, so credentials embedded in connection strings are serialized into `ServiceIR` artifacts and downstream manifests. |
| BUG-155 | Medium | `libs/extractors/graphql.py` | `_parse_default_value()` treats GraphQL default literals as JSON and otherwise returns the raw string, so enum/object/list defaults from introspection are misparsed instead of preserved as structured GraphQL defaults. |
| BUG-156 | Medium | `libs/extractors/graphql.py` | `_selection_set_for_type()` only expands `OBJECT` and `INTERFACE` outputs; fields returning `UNION` types get no selection set, which produces invalid GraphQL documents because union/object outputs require subfield selections. |
| BUG-157 | Medium | `libs/extractors/jsonrpc.py`, `apps/mcp_runtime/proxy.py` | `JsonRpcExtractor` uses `source.auth_header` / `source.auth_token` to fetch the spec but always emits `AuthConfig.none`, so auth-protected JSON-RPC services can be discovered but later invoked by the runtime without the required authorization. |
| BUG-158 | Medium | `libs/extractors/odata.py`, `apps/mcp_runtime/proxy.py` | `ODataExtractor` sends source auth while fetching `$metadata` but serializes `auth=AuthConfig.none`, so protected OData services lose their known auth contract between extraction and runtime invocation. |
| BUG-159 | Medium | `libs/extractors/scim.py`, `apps/mcp_runtime/proxy.py` | `SCIMExtractor` uses auth headers/tokens to read SCIM discovery documents but still emits `AuthConfig.none`, so compiled runtimes call protected SCIM APIs unauthenticated even when extraction succeeded with credentials. |
| BUG-160 | Medium | `libs/extractors/soap.py`, `apps/mcp_runtime/proxy.py` | `SOAPWSDLExtractor` reuses source auth to fetch WSDLs but hardcodes `AuthConfig.none` in the extracted IR, so auth-protected SOAP endpoints become unusable after compilation because the runtime omits the same auth on invocation. |
| BUG-161 | High | `apps/mcp_runtime/main.py`, `apps/mcp_runtime/sql.py` | `build_runtime_state()` instantiates `SQLRuntimeExecutor(service_ir)` without guarding `ToolError`; any loaded IR containing SQL ops plus an unsupported `base_url` scheme causes app construction to raise and crash the runtime instead of surfacing a clean `/readyz` load error. |
| BUG-162 | Medium | `apps/mcp_runtime/sql.py` | `_json_safe_value()` converts every `Decimal` to Python `float`, so high-precision SQL numerics are silently rounded in runtime responses. |
| BUG-163 | Medium | `libs/extractors/sql.py`, `apps/mcp_runtime/sql.py` | `SQLExtractor` always adds a reserved `limit` parameter even when the reflected table already has a `limit` column; at runtime `arguments["limit"]` is consumed both as row-limit control and as a column filter, so callers cannot independently filter that column. |
| BUG-164 | Medium | `apps/mcp_runtime/grpc_unary.py`, `apps/mcp_runtime/grpc_stream.py`, `apps/mcp_runtime/tests/test_grpc_unary.py` | `_method_full_name()` only splits on the first slash and accepts paths like `/pkg.Svc/Method/Extra`, producing an invalid method symbol (`pkg.Svc.Method/Extra`) instead of rejecting malformed gRPC rpc paths early. |
| BUG-165 | High | `apps/mcp_runtime/grpc_unary.py` | `ReflectionGrpcUnaryExecutor` builds channels solely from `service_ir.base_url` and never consults `service_ir.auth`, so native unary gRPC runtime cannot send bearer/basic/api-key/custom-header credentials or mTLS configuration even though the generic HTTP proxy supports those auth modes. |
| BUG-166 | High | `apps/mcp_runtime/grpc_stream.py` | `ReflectionGrpcStreamExecutor` likewise ignores `service_ir.auth`, so native gRPC streaming calls cannot use configured auth metadata or mTLS credentials against protected upstreams. |
| BUG-167 | Medium | `apps/mcp_runtime/grpc_stream.py` | `ReflectionGrpcStreamExecutor` turns `idle_timeout_seconds` into a single hard RPC deadline (`idle_timeout_seconds * max_messages`), so the configured "idle timeout" is never enforced per-message and long-lived active streams can still fail at an unrelated global deadline. |
| BUG-168 | High | `apps/access_control/gateway_binding/client.py`, `apps/access_control/main.py` | `load_gateway_admin_client_from_env()` silently falls back to `InMemoryAPISIXAdminClient` when `GATEWAY_ADMIN_URL` is unset, so the access-control service can report healthy and "sync" PATs/policies/routes successfully while never touching a real gateway. |
| BUG-169 | High | `apps/access_control/audit/routes.py` | `list_audit_logs()` only checks that the caller presents a valid token; it never enforces role, subject, or scope restrictions, so any valid PAT/JWT can read the entire audit log. |
| BUG-170 | High | `apps/access_control/authz/routes.py`, `apps/access_control/authz/models.py` | Policy create/update audit entries use `payload.created_by` as the `actor`, so clients can forge arbitrary audit identities by choosing a different `created_by` field in the request body. |
| BUG-171 | Medium | `apps/access_control/authz/routes.py` | `delete_policy()` always records the audit actor as `"system"` instead of the real caller, destroying accountability for policy deletions even if authentication is added later. |
| BUG-172 | Medium | `apps/access_control/authn/service.py`, `apps/access_control/authz/service.py`, `apps/access_control/audit/service.py` | PAT listing, policy listing, and audit-log listing all hard-cap results at 1000 rows with no pagination API or truncation signal, so callers silently receive incomplete security data sets. |
| BUG-173 | High | `apps/access_control/authn/service.py` | `AuthnService.create_pat()` auto-creates a missing `User` record via `_get_or_create_user()` instead of failing for unknown principals, so PAT creation can bootstrap arbitrary new identities from untrusted input. |
| BUG-174 | Medium | `apps/access_control/authz/service.py` | `update_policy()` allows `payload.created_by` to overwrite the stored `Policy.created_by`, so callers can retroactively rewrite provenance on existing policies and the mirrored gateway binding documents. |
| BUG-179 | High | `deploy/helm/tool-compiler/templates/apps.yaml` | The Helm chart still renders runtime IR as plain `data.service-ir.json` and mounts `SERVICE_IR_PATH=/config/service-ir.json`, so chart-based runtime deployments are stale against the generator/runtime contract that now emits gzipped `binaryData/service-ir.json.gz`. |
| BUG-180 | High | `deploy/helm/tool-compiler/values.yaml`, `deploy/helm/tool-compiler/templates/apps.yaml`, `apps/access_control/gateway_binding/client.py` | Helm defaults `gatewayAdminMock.enabled: false` and only injects `GATEWAY_ADMIN_URL` when that mock is enabled; a default chart install therefore boots access-control with no real gateway URL and silently falls back to the in-memory fake gateway client. |
| BUG-181 | Medium | `deploy/helm/tool-compiler/templates/apps.yaml`, `deploy/docker-compose.yaml`, `apps/access_control/main.py` | Both the Helm chart and local Docker Compose health checks probe access-control on `/healthz` instead of `/readyz`, so the service is marked healthy even when its DB-backed readiness check would report `not_ready`. |
| BUG-182 | Medium | `apps/compiler_api/main.py`, `deploy/helm/tool-compiler/templates/apps.yaml`, `deploy/docker-compose.yaml` | `compiler_api` exposes only `/healthz` and no readiness endpoint or dependency check, so deployments cannot distinguish "process is running" from "API can actually talk to its database/dispatcher." |
| BUG-183 | High | `deploy/k8s/real-targets/seed-all.sh` | When Gitea token creation fails, the seeding script writes the literal placeholder `seed-token-not-available` into Secret `gitea-api-token`, so downstream consumers receive a bogus credential instead of a hard failure. |
| BUG-184 | Medium | `deploy/k8s/real-targets/seed-all.sh` | The Gitea seeding flow suppresses errors for user/org/repo creation with `|| true` and then logs success unconditionally, so partial or total seeding failure is silently reported as a healthy seeded target. |
| BUG-185 | Medium | `deploy/k8s/real-targets/seed-all.sh` | The Directus and PocketBase seeding paths likewise ignore field/record creation failures with `|| true` and still emit `log_ok`, so the script can declare those targets seeded while critical collections or rows were never created. |
| BUG-186 | High | `apps/compiler_worker/celery_app.py` | `execute_compilation()` catches all exceptions, logs them, and returns an `{error, error_type}` payload instead of re-raising, so failed Celery tasks are reported to the broker/backend as successful task completions. |
| BUG-187 | High | `apps/compiler_worker/celery_app.py` | `create_celery_app()` silently falls back to `memory://` and `cache+memory://` when broker/backend env vars are absent, so a misconfigured worker can start "successfully" while queued jobs and results are lost on restart. |
| BUG-188 | Medium | `apps/compiler_worker/entrypoint.py`, `apps/compiler_worker/tests/test_entrypoint.py` | `_broker_endpoint()` only recognizes `redis://` and `rediss://`; when Celery is configured with another supported broker such as AMQP, `_wait_for_broker_socket()` skips readiness checks entirely and the supervisor no longer waits for the actual broker. |
| BUG-189 | High | `libs/generator/generic_mode.py`, `libs/generator/templates/networkpolicy.yaml.j2` | `_upstream_port()` defaults unknown URL schemes to port `443`, so generated network policies allow only TCP/443 egress for protocols like `postgresql://` or `mysql://` when the URL omits an explicit port, breaking runtime connectivity to default database ports such as `5432`. |
| BUG-190 | Medium | `libs/generator/generic_mode.py` | `_sanitize_dns_label()` uses `char.isalnum()`, which treats many Unicode letters as valid; Kubernetes DNS labels are ASCII-only, so non-ASCII service names can produce invalid manifest resource names instead of being normalized safely. |
| BUG-191 | Medium | `apps/compiler_worker/repository.py`, `apps/compiler_worker/models.py` | `SQLAlchemyCompilationJobStore.create_job()` returns an existing job whenever the caller supplies a colliding `job_id`, without checking that `source_url`, `source_hash`, or options match, so distinct compilation requests can be silently aliased onto the wrong persisted job. |
| BUG-192 | High | `apps/compiler_worker/activities/production.py`, `apps/compiler_worker/workflows/compile_workflow.py` | `deploy_rollback()` only logs a warning and returns when `rollback_payload` is missing or malformed, so the workflow can emit rollback success and finish in `rolled_back` state even though deployed manifests were never reverted. |
| BUG-193 | High | `apps/compiler_worker/activities/production.py`, `apps/compiler_worker/workflows/compile_workflow.py` | `route_rollback()` likewise turns invalid `route_config` rollback payloads into a warning plus early return, so failed route publications can be reported as successfully rolled back while gateway state remains unchanged. |
| BUG-194 | High | `apps/compiler_worker/workflows/rollback_workflow.py`, `apps/compiler_worker/tests/test_rollback_workflow.py` | `RollbackWorkflow.run()` deploys the target version before validation/activation and has no compensating restore path; if validation or activation fails, the cluster can stay on the target version while the registry still reports the previous version as active. |
| BUG-195 | Medium | `apps/compiler_worker/main.py` | Worker `/readyz` always returns HTTP 200 even when it sets `"status": "not_ready"` and reports missing configuration, so Kubernetes or load balancers can treat an unconfigured worker as ready. |
| BUG-196 | Medium | `apps/compiler_worker/main.py`, `apps/compiler_worker/entrypoint.py`, `apps/compiler_worker/celery_app.py` | Worker `/readyz` is only an environment echo; it never checks broker reachability, queue registration, or whether a live Celery worker can consume jobs, so readiness can stay green while compilations are not executable. |
| BUG-197 | High | `apps/proof_runner/live_llm_e2e.py` | `run_proofs()` converts per-case exceptions into `ProofResult.error` records and `_async_main()` always just prints JSON, so proof runs exit successfully even when one or more proof cases failed. |
| BUG-198 | Medium | `apps/proof_runner/live_llm_e2e.py`, `apps/proof_runner/tests/test_live_llm_e2e_coverage.py` | `_build_llm_judge_from_env()` catches import/env/client-construction failures and returns `None`, so `--enable-llm-judge` silently degrades to "no judge" instead of failing fast when judge setup is broken. |
| BUG-199 | Medium | `apps/proof_runner/live_llm_e2e.py`, `apps/proof_runner/tests/test_live_llm_e2e_coverage.py` | `_run_case()` swallows `llm_judge.evaluate()` exceptions and continues with `judge_evaluation=None`, so judge-enabled proof runs can report success while omitting verdicts for the affected protocol cases. |
| BUG-200 | High | `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py`, `apps/compiler_worker/models.py` | The frontend `CompilationStatus` model expects uppercase per-stage values such as `DETECTING`, `PUBLISHED`, and `ROLLING_BACK`, but the backend only emits lifecycle values like `pending`, `running`, `succeeded`, `failed`, and `rolled_back`; real jobs therefore never match the UI's polling, filtering, button, or timeline logic. |
| BUG-201 | High | `apps/web-ui/src/components/compilations/status-badge.tsx`, `apps/compiler_api/models.py` | `StatusBadge` indexes `statusConfig[status]` with no fallback, so real backend statuses such as `running` or `succeeded` produce `undefined` and crash the component when it reads `config.className`. |
| BUG-202 | High | `apps/web-ui/src/types/api.ts`, `apps/compiler_api/models.py`, `apps/web-ui/src/components/compilations/event-log.tsx` | The frontend event model expects `{ type, timestamp, detail: string }` plus snake_case names, but backend SSE/API events use `{ event_type, created_at, detail: object }` with dotted names like `stage.started`; even after transport issues are fixed, the event log cannot map colors/labels and may call `.replace()` on `undefined`. |
| BUG-203 | High | `apps/web-ui/src/types/api.ts`, `libs/registry_client/models.py`, `apps/web-ui/src/app/(dashboard)/services/[serviceId]/page.tsx`, `apps/web-ui/src/app/(dashboard)/services/[serviceId]/versions/page.tsx`, `apps/web-ui/src/app/(dashboard)/services/[serviceId]/review/page.tsx` | Frontend artifact version types expect an `ir` field, but the backend returns `ir_json`; service detail, review, and version pages therefore treat real versions as missing IR or read `undefined` instead of the stored ServiceIR. |
| BUG-204 | High | `apps/web-ui/src/types/api.ts`, `libs/registry_client/models.py`, `apps/web-ui/src/components/services/version-diff.tsx` | The diff UI expects `added_operations`/`removed_operations` to be full `Operation[]` objects and each change to expose `field`, but the backend diff API returns operation-id strings plus change objects keyed as `field_name`, `operation_name`, `added_params`, and `removed_params`; real diffs cannot be rendered correctly. |
| BUG-205 | Medium | `apps/web-ui/src/app/(dashboard)/services/[serviceId]/page.tsx`, `apps/web-ui/src/app/(dashboard)/services/[serviceId]/versions/page.tsx`, `apps/compiler_api/routes/artifacts.py` | Both version-management screens show Delete flows, but one only sleeps and the other only re-fetches the version; neither ever calls the real `DELETE /api/v1/artifacts/{service_id}/versions/{version_number}` endpoint, so deletion is a UI-only no-op. |
| BUG-206 | Medium | `apps/web-ui/src/app/(dashboard)/services/[serviceId]/page.tsx`, `apps/web-ui/src/app/(dashboard)/services/[serviceId]/versions/page.tsx`, `apps/compiler_api/routes/artifacts.py` | Inactive-version "Activate" buttons render without any handler and never call `POST /api/v1/artifacts/{service_id}/versions/{version_number}/activate`, so users cannot activate a stored version from the UI. |
